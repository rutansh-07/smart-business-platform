{
  "name": "smart-business-platform",
  "version": "1.0.0",
  "description": "SmartBiz MERN Stack Enterprise Platform",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm install --prefix backend --omit=dev && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "node backend/server.js",
    "postinstall": "npm install --prefix backend --omit=dev"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
